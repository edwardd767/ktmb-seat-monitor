name: KTMB Trip Opening Monitor (Paused)

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: KTMB monitor is paused
        run: |
          echo "KTMB Trip Opening Monitor is currently paused."
          echo "No KTMB trip checks are being performed and no alert emails will be sent."
          echo "The repository and monitoring code have been kept for future use."
