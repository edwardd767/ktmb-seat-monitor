name: KTMB Seat Monitor (Paused)

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: KTMB monitor is paused
        run: |
          echo "KTMB Seat Monitor is currently paused."
          echo "No KTMB seat checks are being performed and no alert emails will be sent."
          echo "The repository and monitoring code have been kept for future use."
